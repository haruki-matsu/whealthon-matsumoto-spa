version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./vue:/app
    ports:
      - "5173:5173"
    command: /bin/sh -c "npm install && npm run dev -- --host 0.0.0.0"
